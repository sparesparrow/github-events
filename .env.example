# Example environment variables for GitHub Events Monitor

# Path to the SQLite database file
DATABASE_PATH=data/github_events.db

# GitHub Personal Access Token (required for API access)
# Generate one at https://github.com/settings/tokens with 'public_repo' scope
GITHUB_TOKEN=ghp_YourPersonalAccessTokenHere

# Polling interval in seconds (how often to fetch new events from GitHub API)
POLL_INTERVAL=60

# Optional: API port for FastAPI
API_PORT=8000
